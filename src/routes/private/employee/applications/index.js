import React from "react";

// redux
// import { useSelector } from "react-redux";

const Applications = () => {
  // const applications = useSelector((state) => state.user.applications);
  // hit an endpoint that will take in an array of ids, which will represent the applications

  return (
    <div>
      <h2>Applications Component</h2>
    </div>
  );
};

export default Applications;
